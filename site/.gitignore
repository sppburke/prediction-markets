# Next.js / Node
/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo

# Local env (never commit the anon key file; see .env.example)
.env
.env.local
.env*.local
# Production secrets (AUTH_*, service-role key) live here on the VPS — never commit (#398 WS3)
.env.production

# Misc
.DS_Store
npm-debug.log*
